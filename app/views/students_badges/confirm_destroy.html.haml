-# Either the layout's shared `modal` frame or a page — never both.
- content_for :title, "Odbierz odznakę #{@students_badge.name}"

- if @in_modal
  = turbo_frame_tag 'modal' do
    = render 'confirm_destroy', students_badge: @students_badge
- else
  %section.gh-panel.gh-dialog-page-panel
    = render 'confirm_destroy', students_badge: @students_badge
