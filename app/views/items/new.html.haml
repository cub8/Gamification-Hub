- content_for :title, "New Item"

.dark-page
  .row.justify-content-center
    .col-12.col-md-10.col-lg-8
      %h1.sg-form-title New Item

      - content_for :form_cancel do
        = link_to "Cancel", story_group_items_path(@story_group), class: "sg-btn-cancel"

      = render "form", item: @item
