- content_for :title, "New Item"

.dark-page
  .row.justify-content-center
    .col-12.col-md-10.col-lg-8
      %h1.page-title.mb-4 New Item

      - content_for :form_cancel do
        = link_to "Cancel", story_group_items_path(@story_group), class: "btn-cancel py-2 px-3"

      = render "form", item: @item
