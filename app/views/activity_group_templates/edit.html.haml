-# Mockup: #/t/sheet-template-edit, js-expanded/30-ag.js (view `edittpl`).
- content_for :title, "Edytuj szablon #{@activity_group_template.base_name}"

= render 'shared/sheet_form', record: @activity_group_template, variant: :template_edit,
  sheet_names: @sheet_names
