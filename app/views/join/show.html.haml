- content_for :title, 'Dołącz do grupy'

- if @in_modal
  = turbo_frame_tag 'modal' do
    = render 'join/step_nickname', story_group: @story_group, code: @code, result: @result
- else
  = render 'join/step_nickname', story_group: @story_group, code: @code, result: @result
