- if !user_in_group(@current_user, @invite.story_group)
  %h3= "You are about to join #{@invite.story_group.name}"

  %p
    = button_to "Join",
      join_index_path(code: @invite.code),
      method: :post

  %p
    = link_to "Back", home_path

- else
  %h3= "You are already part of this group"
