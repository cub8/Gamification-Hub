- if !user_in_group(@current_user, @invite.story_group)
  %h3= "Dołączasz do grupy #{@invite.story_group.name}"

  %p
    = button_to "Dołącz",
      join_index_path(code: @invite.code),
      method: :post

  %p
    = link_to "Wróć", home_path

- else
  %h3= "Jesteś już członkiem tej grupy"
