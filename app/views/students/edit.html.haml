- content_for :title, "Edit Lives — #{@student.full_name}"

.dark-page
  .row.justify-content-center
    .col-12.col-md-8.col-lg-6
      %h1.page-title.mb-4 Edit Lives

      - if @student.errors.any?
        .form-errors.p-3.mb-3
          %h2 Please fix the following errors:
          %ul
            - @student.errors.full_messages.each do |message|
              %li= message

      = form_with model: @student, url: story_group_student_path(@story_group, @student), method: :patch do |f|
        .form-section.p-4.mb-3
          .section-title.mb-3
            = @student.full_name
            %small.text-dimmed.fw-normal.ms-2= @student.email

          .field-group.mb-3
            = f.label :lives, "Lives"
            = f.number_field :lives, class: "form-control", min: 0

        .form-actions.gap-3.mt-4
          = link_to "Cancel", story_group_students_path(@story_group), class: "btn-cancel py-2 px-3"
          = f.submit "Save", class: "btn-submit py-2 px-4"
