- content_for :title, "New story group"

%turbo-frame#modal
  .sg-modal-backdrop
    .sg-form-card
      %h1.sg-form-title New Story Group
      = render "form", story_group: @story_group, cancel_path: story_groups_path, submit_label: "Create"
