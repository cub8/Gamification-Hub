- content_for :title, "Edit story group"

%turbo-frame#modal
  .sg-modal-backdrop
    .sg-form-card
      %h1.sg-form-title Edit Story Group
      = render "form", story_group: @story_group, cancel_path: story_group_path(@story_group), submit_label: "Save"
