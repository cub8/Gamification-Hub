- content_for :title, "Edytuj grupę — #{@activity_group.name}"

= turbo_frame_tag 'modal' do
  .modal-overlay.py-5.px-3
    .form-card.p-4
      = render MessagesComponent.new(id: 'modal-messages', flash: flash, notice: notice, alert: alert)
      %h2.page-title.mb-4 Edytuj Grupę Aktywności
      = render 'form'
