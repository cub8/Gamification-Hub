-# Sheet settings. Mockup: #/t/sheet-settings, js-expanded/30-ag.js (view `group`).
- content_for :title, "Ustawienia arkusza #{@activity_group.name}"

= render 'shared/redesign/sheet_form', record: @activity_group, variant: :sheet,
  awards_count: @activity_group.activity_group_categories.sum(&:awards_count)
