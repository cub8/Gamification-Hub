-# A badge as a mini entity card — the mockup's mini('badge', glyph)
-# (30-lists.js:15). Teal, because a badge is teal in this design language
-# (DECISIONS.md:22).
%span.gh-card.gh-card--badge.gh-mc
  %span.gh-card-f
    %span.gh-card-i{ data: local_assigns[:inner_data] }
      = render 'art', badge: badge
