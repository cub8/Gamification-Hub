- content_for :title, "New Badge"

.dark-page
  .row.justify-content-center
    .col-12.col-md-8.col-lg-6
      %h1.sg-form-title New Badge

      - content_for :form_cancel do
        = link_to "Cancel", story_group_badges_path(@story_group), class: "sg-btn-cancel"

      = render "form", badge: @badge
