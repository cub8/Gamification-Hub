- content_for :title, "New Badge"

.dark-page
  .row.justify-content-center
    .col-12.col-md-8.col-lg-6
      %h1.page-title.mb-4 New Badge

      - content_for :form_cancel do
        = link_to "Cancel", story_group_badges_path(@story_group), class: "btn-cancel py-2 px-3"

      = render "form", badge: @badge
