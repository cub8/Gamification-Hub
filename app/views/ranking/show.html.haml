%h1 Ranking

= link_to 'Back', story_group_path(@story_group)

%hr

- if @students.empty?
  %p No students yet.
- else
  %table.table.table-sm.table-bordered
    %thead
      %tr
        %th Position
        %th Name
        %th Total currency
    %tbody
      - @students.each_with_index do |student, index|
        %tr
          %td= index + 1
          %td= student.full_name
          %td= student.total_currency
